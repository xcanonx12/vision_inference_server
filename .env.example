# SimpleInference environment variables.
# Copy this file to `.env` and adjust as needed. Values here are read by
# docker-compose and by the server at startup.

# Path to the YAML config file the server loads.
CONFIG_PATH=server/config.yaml

# Logging verbosity: DEBUG, INFO, WARNING, ERROR, or CRITICAL.
LOG_LEVEL=INFO
