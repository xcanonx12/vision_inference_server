# Core gRPC + serving
grpcio==1.78.0
grpcio-tools==1.78.0
fastapi==0.136.3
uvicorn==0.48.0
pydantic==2.13.4
httpx==0.28.1
pyyaml==6.0.3

# Vision / inference
torch==2.11.0
torchvision==0.26.0
ultralytics==8.4.55
rfdetr==1.7.0
supervision==0.28.0
opencv-python==4.13.0.92
numpy==2.4.3
onnxruntime==1.26.0

# Testing
pytest==9.0.3
pytest-cov==7.1.0
pytest-asyncio==1.4.0

# GPU note: the pins above install CPU torch from PyPI. For CUDA (e.g. RTX GPUs
# on driver 12.x), reinstall the matching build:
#   pip install --reinstall torch==2.11.0 torchvision==0.26.0 \
#       --index-url https://download.pytorch.org/whl/cu128
